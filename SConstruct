VariantDir('build_std','src')
SConscript('build_std/SConscript_std')

#env = Environment()
#env.BuildDir('build/', 'src/')
#environment.Library(target="Foo", source=["src/foo.spp"])
#Export('env')
#SConscript('SConscript', build_dir='build', duplicate=0, exports={'MODE':'release'})
#SConscript('SConscript', build_dir='build', duplicate=0, exports={'MODE':'debug'})

